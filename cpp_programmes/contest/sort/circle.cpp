#include <bits/stdc++.h> 

using namespace std;


int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr); std::cout.tie(nullptr);

    long long l, n, k;
    cin >> l >> n >> k;

    vector<long long> idx(n);
    for (int i = 0; i < n; ++i) {
        cin >> idx[i];
    }

    
}